import React from "react";

//This is to list all the information about a client from simply clicking on their name in index, profile or client list
//Nothing can be done until the information is actually displayed
const ClientInfoList = () => {
  return <div>ClientInfoList</div>;
};

export default ClientInfoList;
